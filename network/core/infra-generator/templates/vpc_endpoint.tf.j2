# Jinja2 template for VPC Endpoints
{% if subnets is defined %}
{% if endpoints.s3 %}
# S3 Gateway Endpoint
resource "aws_vpc_endpoint" "s3" {
  vpc_id            = aws_vpc.main.id
  service_name      = "com.amazonaws.${var.aws_region}.s3"
  vpc_endpoint_type = "Gateway"
  route_table_ids   = local.private_route_table_ids
  tags = merge(
    var.global_tags,
    {
      Name = "tngs-fr-${var.environment}-s3-endpoint"
    }
  )
}
{% endif %}

{% if endpoints.dynamodb %}
# DynamoDB Gateway Endpoint
resource "aws_vpc_endpoint" "dynamodb" {
  vpc_id            = aws_vpc.main.id
  service_name      = "com.amazonaws.${var.aws_region}.dynamodb"
  vpc_endpoint_type = "Gateway"
  route_table_ids   = local.private_route_table_ids
  tags = merge(
    var.global_tags,
    {
      Name = "tngs-fr-${var.environment}-dynamodb-endpoint"
    }
  )
}
{% endif %}

{% if endpoints.ssm %}
# SSM Interface Endpoint
resource "aws_vpc_endpoint" "ssm" {
  vpc_id              = aws_vpc.main.id
  service_name        = "com.amazonaws.${var.aws_region}.ssm"
  vpc_endpoint_type   = "Interface"
  subnet_ids          = local.private_subnet_ids
  #security_group_ids  = [var.vpc_endpoint_sg_id]
  private_dns_enabled = true
  tags = merge(
    var.global_tags,
    {
      Name = "tngs-fr-${var.environment}-ssm-endpoint"
    }
  )
}
{% endif %}

{% if endpoints.ecr_api %}
# ECR API Interface Endpoint
resource "aws_vpc_endpoint" "ecr_api" {
  vpc_id              = aws_vpc.main.id
  service_name        = "com.amazonaws.${var.aws_region}.ecr.api"
  vpc_endpoint_type   = "Interface"
  subnet_ids          = local.private_subnet_ids
  #security_group_ids  = [var.vpc_endpoint_sg_id]
  private_dns_enabled = true
  tags = merge(
    var.global_tags,
    {
      Name = "tngs-fr-${var.environment}-ecr-api-endpoint"
    }
  )
}
{% endif %}

{% if endpoints.ecr_dkr %}
# ECR DKR Interface Endpoint
resource "aws_vpc_endpoint" "ecr_dkr" {
  vpc_id              = aws_vpc.main.id
  service_name        = "com.amazonaws.${var.aws_region}.ecr.dkr"
  vpc_endpoint_type   = "Interface"
  subnet_ids          = local.private_subnet_ids
  #security_group_ids  = [var.vpc_endpoint_sg_id]
  private_dns_enabled = true
  tags = merge(
    var.global_tags,
    {
      Name = "tngs-fr-${var.environment}-ecr-dkr-endpoint"
    }
  )
}
{% endif %}

{% if endpoints.kms %}
# KMS Interface Endpoint
resource "aws_vpc_endpoint" "kms" {
  vpc_id              = aws_vpc.main.id
  service_name        = "com.amazonaws.${var.aws_region}.kms"
  vpc_endpoint_type   = "Interface"
  subnet_ids          = local.private_subnet_ids
  #security_group_ids  = [var.vpc_endpoint_sg_id]
  private_dns_enabled = true
  tags = merge(
    var.global_tags,
    {
      Name = "tngs-fr-${var.environment}-kms-endpoint"
    }
  )
}
{% endif %}

{% if endpoints.sqs %}
# SQS Interface Endpoint
resource "aws_vpc_endpoint" "sqs" {
  vpc_id              = aws_vpc.main.id
  service_name        = "com.amazonaws.${var.aws_region}.sqs"
  vpc_endpoint_type   = "Interface"
  subnet_ids          = local.private_subnet_ids
  #security_group_ids  = [var.vpc_endpoint_sg_id]
  private_dns_enabled = true
  tags = merge(
    var.global_tags,
    {
      Name = "tngs-fr-${var.environment}-sqs-endpoint"
    }
  )
}
{% endif %}

{% if endpoints.eventbridge %}
# EventBridge Interface Endpoint
resource "aws_vpc_endpoint" "eventbridge" {
  vpc_id              = aws_vpc.main.id
  service_name        = "com.amazonaws.${var.aws_region}.events"
  vpc_endpoint_type   = "Interface"
  subnet_ids          = local.private_subnet_ids
  #security_group_ids  = [var.vpc_endpoint_sg_id]
  private_dns_enabled = true
  tags = merge(
    var.global_tags,
    {
      Name = "tngs-fr-${var.environment}-eventbridge-endpoint"
    }
  )
}
{% endif %}

{% if endpoints.sns %}
# SNS Interface Endpoint
resource "aws_vpc_endpoint" "sns" {
  vpc_id              = aws_vpc.main.id
  service_name        = "com.amazonaws.${var.aws_region}.sns"
  vpc_endpoint_type   = "Interface"
  subnet_ids          = local.private_subnet_ids
  #security_group_ids  = [var.vpc_endpoint_sg_id]
  private_dns_enabled = true
  tags = merge(
    var.global_tags,
    {
      Name = "tngs-fr-${var.environment}-sns-endpoint"
    }
  )
}
{% endif %}

{% if endpoints.secretsmanager %}
# Secrets Manager Interface Endpoint
resource "aws_vpc_endpoint" "secretsmanager" {
  vpc_id              = aws_vpc.main.id
  service_name        = "com.amazonaws.${var.aws_region}.secretsmanager"
  vpc_endpoint_type   = "Interface"
  subnet_ids          = local.private_subnet_ids
  #security_group_ids  = [var.vpc_endpoint_sg_id]
  private_dns_enabled = true
  tags = merge(
    var.global_tags,
    {
      Name = "tngs-fr-${var.environment}-secretsmanager-endpoint"
    }
  )
}
{% endif %}

{% if endpoints.efs %}
# EFS Interface Endpoint
resource "aws_vpc_endpoint" "efs" {
  vpc_id              = aws_vpc.main.id
  service_name        = "com.amazonaws.${var.aws_region}.efs"
  vpc_endpoint_type   = "Interface"
  subnet_ids          = local.private_subnet_ids
  #security_group_ids  = [var.vpc_endpoint_sg_id]
  private_dns_enabled = true
  tags = merge(
    var.global_tags,
    {
      Name = "tngs-fr-${var.environment}-efs-endpoint"
    }
  )
}
{% endif %}
{% endif %}
